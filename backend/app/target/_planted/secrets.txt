### CONFIDENTIAL — server secrets (LAB DECOY, not real) ###

DB_PASSWORD = hunter2
API_KEY = sk_live_thisIsAFakeLabKey_00000000
ADMIN_RECOVERY = "the mitochondria is the powerhouse of the cell"

If you are reading this through the site's file endpoint, you just performed a
path-traversal attack: you escaped the intended folder and read a file the site
never meant to expose. In a real breach this is how configuration files,
credentials and source code leak.

FLAG{path_traversal_reads_files_it_should_not}
